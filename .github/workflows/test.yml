name: Demo pipeline

on: [push]

jobs:
  deploy:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Slack CLI
        run: curl -fsSL https://downloads.slack-edge.com/slack-cli/install-dev.sh | bash
      - name: Check slack cli version
        run: slack-dev --version
      - name: Try Slack CLI login
        env:
          SUPER_SECRET: ${{ secrets.SLACK_TOKEN }}
        run: slack-dev login --auth "${{ secrets.SLACK_TOKEN }}"
      - name: Create an App from template for Demo purpose
        run: slack-dev create my-app -t slack-samples/deno-reverse-string 
      - name: Start Slack CLI commands with the newly created app
        run: cd my-app && ls -l
      - name: Start Slack CLI commands with the newly created app
        run: echo $(hermes-dev run --token "xoxp-3290231583797-3918262610754-5342498098241-6408b14b5addd8c1a010c6cdcc1a074a") | xdotool key Return
      
      
